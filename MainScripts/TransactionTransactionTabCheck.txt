#NoEnv  ; Recommended for performance and compatibility with future AutoHotkey releases.
; #Warn  ; Enable warnings to assist with detecting common errors.
SendMode Input  ; Recommended for new scripts due to its superior speed and reliability.
SetWorkingDir %A_ScriptDir%  ; Ensures a consistent starting directory.

; reminder to replace needles with actual unique placeholders and to update script setup.ahk with those placeholders.

PixelSearch, MatchCoordX, MatchCoordY, TransactionTransactionTabCheckX1, TransactionTransactionTabCheckY1, TransactionTransactionTabCheckX1 + 2, TransactionTransactionTabCheckY1 + 2, TransactionTransactionTabCheckColor, 5
	If ErrorLevel 0
		Exit